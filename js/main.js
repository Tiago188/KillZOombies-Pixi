/*global PIXI*/

(function(_super) {
    'use strict';

    //Create a container object called the `stage`
    //kz.stage = new kz.Stage(window.innerWidth, window.innerHeight);
    kz.stage = new kz.Stage(600, window.innerHeight);

    var load = new kz.Load();
}(this));
